server {
    listen 80;
    server_name engchatt.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name engchatt.com;

    ssl_certificate     /etc/ssl/ec-landingpage.pem;
    ssl_certificate_key /etc/ssl/ec-landingpage.key;

    root /usr/share/nginx/html;
    index index.html;

    location ~ \.css {
      add_header Content-Type text/css;
    }
    
    location ~ \.js {
      add_header Content-Type application/x-javascript;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /eula {
        rewrite ^/eula$ /eula.html break;
     }

    location /privacy {
        rewrite ^/privacy$ /privacy.html break;
     }
}